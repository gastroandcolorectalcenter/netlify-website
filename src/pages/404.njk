---
layout: layouts/base.njk
title: 404 - Page Not Found
description: The page you're looking for cannot be found. Return to our homepage or contact us for assistance.
permalink: /404.html
eleventyExcludeFromCollections: true
---

<div class="error-page">
  <div class="container">
    <div class="error-content">
      {# Error Illustration/Icon #}
      <div class="error-icon">
        <svg xmlns="http://www.w3.org/2000/svg" width="120" height="120" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="10"/>
          <line x1="12" y1="8" x2="12" y2="12"/>
          <line x1="12" y1="16" x2="12.01" y2="16"/>
        </svg>
      </div>
      
      {# Error Message #}
      <h1 class="error-title">404</h1>
      <h2 class="error-subtitle">Page Not Found</h2>
      <p class="error-description">
        We're sorry, but the page you're looking for doesn't exist or has been moved. 
        This might have happened because the link is outdated or the URL was mistyped.
      </p>
      
      {# Action Buttons #}
      <div class="error-actions">
        <a href="/" class="btn btn-primary">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>
            <polyline points="9 22 9 12 15 12 15 22"/>
          </svg>
          <span>Back to Home</span>
        </a>
        
        <a href="/#contact" class="btn btn-secondary">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
            <polyline points="22,6 12,13 2,6"/>
          </svg>
          <span>Contact Us</span>
        </a>
      </div>
      
      {# Quick Links #}
      <div class="error-links">
        <h3>Quick Links</h3>
        <ul class="quick-links-list">
          <li>
            <a href="/about/">About Dr. Shah</a>
          </li>
          <li>
            <a href="/#services">Our Services</a>
          </li>
          <li>
            <a href="/appointment/">Book Appointment</a>
          </li>
          <li>
            <a href="/blogs/">Health Blog</a>
          </li>
          <li>
            <a href="/#hospitals">Hospital Locations</a>
          </li>
          <li>
            <a href="/#faq">FAQ</a>
          </li>
        </ul>
      </div>
      
      {# Contact Information #}
      <div class="error-contact">
        <h3>Need Immediate Help?</h3>
        <div class="contact-options">
          <a href="tel:{{ site.contact.phone }}" class="contact-option">
            {% include "svg/phone.njk" %}
            <span>{{ site.contact.phoneDisplay }}</span>
          </a>
          
          <a href="mailto:{{ site.contact.email }}" class="contact-option">
            {% include "svg/mail.njk" %}
            <span>{{ site.contact.email }}</span>
          </a>
          
          <a href="https://wa.me/{{ social.whatsapp.number }}" 
             class="contact-option"
             target="_blank"
             rel="noopener noreferrer">
            {% include "svg/whatsapp.njk" %}
            <span>WhatsApp</span>
          </a>
        </div>
      </div>
      
      {# Search Box (Optional - Can be implemented later) #}
      <div class="error-search">
        <h3>Search Our Site</h3>
        <form action="/" method="get" class="search-form" role="search">
          <input type="search" 
                 name="q" 
                 placeholder="Search for services, doctors, or information..." 
                 class="search-input"
                 aria-label="Search">
          <button type="submit" class="search-button">
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="11" cy="11" r="8"/>
              <path d="m21 21-4.35-4.35"/>
            </svg>
            <span class="sr-only">Search</span>
          </button>
        </form>
      </div>
    </div>
  </div>
</div>

<style>
/* 404 Page Specific Styles */
.error-page {
  min-height: 70vh;
  display: flex;
  align-items: center;
  padding: 4rem 0;
}

.error-content {
  text-align: center;
  max-width: 600px;
  margin: 0 auto;
}

.error-icon {
  color: var(--primary-color, #0066cc);
  margin-bottom: 2rem;
}

.error-title {
  font-size: 6rem;
  font-weight: 700;
  color: var(--primary-color, #0066cc);
  margin: 0;
  line-height: 1;
}

.error-subtitle {
  font-size: 2rem;
  margin: 1rem 0;
  color: var(--text-dark, #1a1a1a);
}

.error-description {
  font-size: 1.1rem;
  color: var(--text-muted, #666);
  margin-bottom: 2rem;
  line-height: 1.6;
}

.error-actions {
  display: flex;
  gap: 1rem;
  justify-content: center;
  margin-bottom: 3rem;
  flex-wrap: wrap;
}

.error-links,
.error-contact,
.error-search {
  margin-top: 3rem;
  padding-top: 3rem;
  border-top: 1px solid #e5e5e5;
}

.error-links h3,
.error-contact h3,
.error-search h3 {
  font-size: 1.25rem;
  margin-bottom: 1.5rem;
  color: var(--text-dark, #1a1a1a);
}

.quick-links-list {
  list-style: none;
  padding: 0;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 0.75rem;
  max-width: 500px;
  margin: 0 auto;
}

.quick-links-list a {
  color: var(--primary-color, #0066cc);
  text-decoration: none;
  padding: 0.5rem;
  display: block;
  border-radius: 4px;
  transition: background-color 0.2s;
}

.quick-links-list a:hover {
  background-color: #f5f5f5;
}

.contact-options {
  display: flex;
  gap: 1.5rem;
  justify-content: center;
  flex-wrap: wrap;
}

.contact-option {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  color: var(--text-dark, #1a1a1a);
  text-decoration: none;
  padding: 0.75rem 1.5rem;
  border: 1px solid #e5e5e5;
  border-radius: 8px;
  transition: all 0.2s;
}

.contact-option:hover {
  border-color: var(--primary-color, #0066cc);
  color: var(--primary-color, #0066cc);
}

.contact-option svg {
  width: 20px;
  height: 20px;
}

.search-form {
  display: flex;
  max-width: 500px;
  margin: 0 auto;
  border: 2px solid #e5e5e5;
  border-radius: 8px;
  overflow: hidden;
}

.search-input {
  flex: 1;
  padding: 0.75rem 1rem;
  border: none;
  font-size: 1rem;
}

.search-input:focus {
  outline: none;
}

.search-button {
  padding: 0.75rem 1.5rem;
  background: var(--primary-color, #0066cc);
  color: white;
  border: none;
  cursor: pointer;
  transition: background-color 0.2s;
}

.search-button:hover {
  background: var(--primary-dark, #0052a3);
}

.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border-width: 0;
}

@media (max-width: 768px) {
  .error-title {
    font-size: 4rem;
  }
  
  .error-subtitle {
    font-size: 1.5rem;
  }
  
  .error-actions {
    flex-direction: column;
  }
  
  .quick-links-list {
    grid-template-columns: 1fr;
  }
  
  .contact-options {
    flex-direction: column;
  }
}
</style>